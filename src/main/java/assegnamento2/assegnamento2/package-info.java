/**
 * The package assignment2 contains the packages: communication, controller and db;
 * it also contains the Store class, which is the one that starts the application.
 */
package assegnamento2.assegnamento2;